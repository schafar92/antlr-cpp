// test comment
for(int i = 0; i < 10; ++i)
  foo();

#pragma omp
for (int i = 0; i < 10; ++i)
  foo();

#pragma omp parallel for
#pragma omp parallel for
for (c[ii] = 0; ii < 10; ++ii)
  c[ii] = a[ii];

#pragma omp parallel for
for (auto &item: a) {
